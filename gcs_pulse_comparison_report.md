# GCS-PULSE: MCP vs CLI 비교 보고서

**작성일:** 2026-04-05  
**환경:** Claude Code (claude-sonnet-4-6), macOS  
**측정 방법:** Anthropic `count_tokens` API 실측

---

## 1. 연결 방식 개요

| 항목 | MCP 방식 | CLI 방식 |
|------|----------|----------|
| 연결 유형 | Streamable HTTP (MCP 프로토콜 2024-11-05) | curl / REST API 직접 호출 |
| 서버 | `https://api.1000.school/mcp` | `https://api.1000.school` |
| 인증 | Bearer Token (MCP 세션 초기화) | Bearer Token (HTTP 헤더) |
| 초기 설정 | `claude mcp add` 명령 필요 | 불필요 (Bash 툴 사용) |
| 등록 툴 수 | 57개 (gcs-pulse-mcp v1.26.0) | 해당 없음 |

---

## 2. 토큰 구조 차이

MCP와 CLI의 토큰 비용이 다른 근본 원인은 **툴 정의 오버헤드**입니다.

| 구성 요소 | MCP | CLI |
|----------|-----|-----|
| 툴 정의 (고정) | 57개 전체 = **~6,531 토큰** | Bash 1개 = **~50 토큰** |
| 사용자 프롬프트 | 동일 | 동일 |
| 툴 호출 (tool_use) | ~30 토큰 | ~30 토큰 |
| API 응답 (tool_result) | 데이터에 따라 동일 | 데이터에 따라 동일 |

> MCP는 57개 툴 정의(26,125 bytes)가 **매 턴마다 컨텍스트에 항상 포함**됩니다.  
> 실제로 사용하는 툴이 1개여도 나머지 56개 정의가 그대로 실립니다.

---

## 3. 실측 비교: "4월 1일 데일리 스니펫 조회해줘"

### 사용자 입력 (동일)
```
4월 1일 데일리 스니펫 조회해줘
```

---

### MCP 방식

Claude가 자연어를 해석해 파라미터를 자동 추론하고 툴을 호출합니다.

**Claude 내부 처리 (자동):**
```json
daily_snippets_list({
  "from_date": "2026-04-01",
  "to_date":   "2026-04-01",
  "scope":     "own"
})
```

- `"4월 1일"` → `"2026-04-01"` 자동 변환
- `from_date`, `to_date`, `scope` 파라미터 자동 선택
- 사용자는 자연어 외에 아무것도 입력하지 않아도 됨

**응답 결과 (주요 필드):**
```json
{
  "id": 3365,
  "date": "2026-04-01",
  "content": "#### 오늘 한 일\n- 클로드 코드 활용하여 아티팩트, 스킬 등 다양한 기능들 경험...",
  "feedback": {
    "total_score": 79,
    "key_learning": "팀 내 생산적 소통과 도구 실전 활용이 도메인 지식의 빈틈을 드러냈다.",
    "mentor_comment": "오늘은 도구를 직접 다뤄보고 실제 팀 결성을 이뤄낸 '전진'의 하루였습니다..."
  }
}
```

**실측 토큰: 10,420 토큰**

---

### CLI 방식

API 스펙을 알고 curl 명령을 직접 구성해야 합니다.

**직접 작성해야 하는 명령:**
```bash
curl -s \
  -H "Authorization: Bearer $TOKEN" \
  "https://api.1000.school/daily-snippets?from_date=2026-04-01&to_date=2026-04-01&scope=own"
```

사용자가 직접 알아야 하는 것:
- `"4월 1일"` → `"2026-04-01"` (YYYY-MM-DD 형식 직접 변환)
- 파라미터 이름: `from_date`, `to_date`, `scope`
- 내 스니펫만 보려면 `scope=own` 필요
- 엔드포인트 경로: `/daily-snippets`

**응답 결과:** MCP와 동일한 JSON

**실측 토큰: 3,405 토큰**

---

### 비교 요약

| 항목 | MCP | CLI |
|------|-----|-----|
| 사용자 입력 | 자연어 그대로 | curl 명령 직접 작성 |
| 날짜 변환 | Claude 자동 (`"4월 1일"` → `"2026-04-01"`) | 사용자 직접 |
| 파라미터 선택 | 스키마 기반 자동 추론 | API 문서 보고 직접 입력 |
| 응답 데이터 | 동일 | 동일 |
| **실측 입력 토큰** | **10,420** | **3,405** |
| **토큰 차이** | 기준 | **MCP 대비 67% 절약** |
| **배율** | 3.1배 | 1배 |

---

## 4. 개발자 경험 비교

### MCP

| | 내용 |
|-|------|
| **장점** | 자연어 명령으로 툴 자동 호출 / 파라미터 자동 추론 / 멀티스텝 작업 시 컨텍스트 유지 |
| **단점** | 57개 툴 정의 고정 비용 (~6,500 토큰) / `claude mcp add` 초기 설정 필요 |

### CLI

| | 내용 |
|-|------|
| **장점** | 툴 정의 오버헤드 없음 / `jq` 등으로 필요한 필드만 추출 가능 / 토큰만 있으면 즉시 사용 |
| **단점** | API 스펙 직접 숙지 필요 / 자연어 해석 불가 / 복잡한 워크플로우에서 스크립트 길어짐 |

---

## 5. 시나리오별 추천

| 시나리오 | 추천 | 이유 |
|---------|------|------|
| 자연어로 즉석 조회 | **MCP** | 파라미터 자동 추론, 편의성 우선 |
| 반복 자동화 / 배치 처리 | **CLI** | 토큰 절약, 파이프라인 조합 |
| 토큰 비용 최소화 | **CLI** | 최대 67% 절약 |
| 멀티스텝 복합 작업 | **MCP** | 컨텍스트 유지, 툴 체인 자동화 |
| 빠른 프로토타이핑 | **CLI** | 설정 없이 즉시 사용 |

---

## 6. 결론

- **토큰 효율:** CLI가 최대 67% 절약 — MCP 57개 툴 정의 고정 비용이 주원인
- **편의성:** MCP가 우위 — 자연어 → 파라미터 자동 추론, API 스펙 불필요
- **권장 전략:**
  - 탐색·대화형 조회 → **MCP**
  - 반복 자동화·비용 민감 구간 → **CLI**
  - 혼합 사용: MCP로 시작 후 고비용 구간을 CLI 스크립트로 교체

---

*측정 환경: Claude Code claude-sonnet-4-6 / GCS-PULSE MCP gcs-pulse-mcp v1.26.0 / 2026-04-05*
